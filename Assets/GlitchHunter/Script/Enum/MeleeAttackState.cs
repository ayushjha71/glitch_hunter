using UnityEngine;

namespace GlitchHunter.Enum
{
    public enum MeleeAttackState
    {
        Default,
        IDLE,
        WALK,
        Attack_ONE,
        Attack_TWO,
        Attack_THREE,
        DEAD
    }
}
